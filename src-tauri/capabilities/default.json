{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main window. Grants the core defaults (event listen/unlisten, read-only window queries) plus the explicit window-write commands used by the custom Win11 titlebar (minimize / toggle-maximize / close) and the unminimize/show/set-focus needed by the single-instance Rust handler. Grants dialog:open for Ctrl+O. Filesystem grants: read-text-file (loading the .md), write-text-file + rename + remove + exists (recent.json atomic write & cleanup, PR-5a; scroll-positions.json same pattern, PR-5b). PR-8: dialog:allow-save for the right-click 'Save image as...' menu, fs:allow-write-file for binary image writes, fs:allow-mkdir + fs:allow-read-dir for the rolling-file logger's data/logs/ directory creation + bak cleanup. PR-9: window:allow-set-fullscreen + window:allow-is-fullscreen for the F11 fullscreen toggle in useShortcuts (R13). PR-9 hotfix: window:allow-set-size + window:allow-set-position + window:allow-maximize for window state persistence restore (R2.8, R10.1); the matching read-only queries (inner-size / outer-position / is-maximized / available-monitors / primary-monitor) are already in core:default. Filesystem scope is intentionally unrestricted in v0.1: the user explicitly chooses every file they open. Shell grant: open (PR-5b) for R7 link routing — http/https/mailto + local-non-md open via system default app; scope is permissive in v0.1 (security model: user authored every link).",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "core:window:allow-minimize",
    "core:window:allow-toggle-maximize",
    "core:window:allow-close",
    "core:window:allow-show",
    "core:window:allow-set-focus",
    "core:window:allow-unminimize",
    "core:window:allow-set-fullscreen",
    "core:window:allow-is-fullscreen",
    "core:window:allow-set-size",
    "core:window:allow-set-position",
    "core:window:allow-maximize",
    "dialog:allow-open",
    "dialog:allow-save",
    {
      "identifier": "fs:allow-read-text-file",
      "allow": [{ "path": "**" }]
    },
    {
      "identifier": "fs:allow-write-text-file",
      "allow": [{ "path": "**" }]
    },
    {
      "identifier": "fs:allow-write-file",
      "allow": [{ "path": "**" }]
    },
    {
      "identifier": "fs:allow-read-dir",
      "allow": [{ "path": "**" }]
    },
    {
      "identifier": "fs:allow-mkdir",
      "allow": [{ "path": "**" }]
    },
    {
      "identifier": "fs:allow-rename",
      "allow": [{ "path": "**" }]
    },
    {
      "identifier": "fs:allow-remove",
      "allow": [{ "path": "**" }]
    },
    {
      "identifier": "fs:allow-exists",
      "allow": [{ "path": "**" }]
    },
    {
      "identifier": "shell:allow-open",
      "allow": [
        { "url": "https://**" },
        { "url": "http://**" },
        { "url": "mailto:*" },
        { "url": "tel:*" },
        { "path": "**" }
      ]
    }
  ]
}
